# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/newways?retryWrites=true&w=majority

# Server Configuration
PORT=4002

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this
TOKEN_EXPIRY=7d

# Frontend CORS Origins (adjust as needed)
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
